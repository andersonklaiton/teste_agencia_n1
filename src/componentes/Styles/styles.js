import styled from "styled-components";

export const EmBreve = styled.div`
        width: 80%;
    margin: 0 auto;
    color: var(--blackblue);
    display: flex;
    flex-direction: column;
    align-items: center;
    font-size: 25px;
    margin-top:30px
`